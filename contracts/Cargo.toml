[package]
name = "casper_accelerate_contract"
version = "0.1.0"
edition = "2021"

[dependencies]
casper-contract = "5.0.0"
casper-types = "6.0.0"
base64ct = "=1.7.2"

[lib]
crate-type = ["cdylib"]
path = "lib.rs"

# Temporarily disabled - uses SDK v6 API
# [[bin]]
# name = "client_deposit"
# path = "client_deposit.rs"
# test = false
# doc = false

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = true

